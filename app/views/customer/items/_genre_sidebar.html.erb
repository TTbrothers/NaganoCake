<div class="container my-lg-5">
  <div class="row">
    <div class="col-lg-3">
      <table class="table">
        <thead>
          <tr>
            <th>ジャンル検索</th>
          </tr>
        </thead>
        <tbody>
          <% genres.each do |genre| %>
          <tr>
            <td>
              <%= link_to genre.name, customer_items_search_path('search[value]': genre.id) %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
